” каждого потока есть им€ и id (числовой идентификатор), их можно узнать:
    long getId() Ч получение идентификатора потока;
    String getName() Ч получение имени потока.
ј им€ установить в конструкторе и после создани€ методом:
    void setName() Ч установка имени потока.
ћетоды управлени€ потоками:
    void interrupt() Ч прерывание выполнени€ потока;
    boolean isAlive() Ч проверка, выполн€етс€ ли поток;
    void join() Ч ожидание завершени€ потока;
    void join(millis) Ч ожидание millis милисекунд завершени€ потока;
    void sleep(int) Ч приостановка потока на заданное врем€;
    void start() Ч запуск потока.
ћетоды дл€ синхронизации потоков:
    void notify() Ч Ђпробуждениеї отдельного потока, ожидающего Ђсигналаї;
    void notifyAll() Ч Ђпробуждениеї всех потоков, ожидающих Ђсигналаї;
    void wait() Ч приостановка потока, пока другой поток не вызовет метод notify();
    void wait(millis) Ч приостановка потока на millis милисекунд, или пока другой поток не вызовет метод notify().