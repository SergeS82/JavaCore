¬ пакете java.util.concurrent.atomic есть классы:
-   AtomicBoolean	—одержит boolean-значение, с которым возможно работать атомарно.
-   AtomicInteger	—одержит integer-значение, с которым возможно работать атомарно.
-   AtomicIntegerArray	—одержит значение массива типа int, с которым возможно работать атомарно.
-   AtomicLong	        —одержит long-значение, с которым возможно работать атомарно.
-   AtomicLongArray	    —одержит значение массива типа long, с которым возможно работать атомарно.
-   AtomicReference<T>	—одержит значение параметризованного типа, с которым возможно работать атомарно.
-   AtomicReferenceArray<T>	—одержит значение массива параметризованного типа, с которым возможно работать атомарно.
ћетоды атомарных классов
-   get()	¬озвращает текущее значение.
-   set()	”станавливает значение, передаваемое в параметр. Ёквивалентно присваиванию (=).
-   getAndSet()	”станавливает новое значение и возвращает предыдущее.
-   incrementAndGet()	ƒл€ числовых типов, пре-инкремент (++value).
-   getAndIncrement()	ƒл€ числовых типов, пост-инкремент (value++).
-   decrementAndGet()	ƒл€ числовых типов, пре-декремент (--value).
-   getAndDecrement()	ƒл€ числовых типов, пост-декремент (value--).
-   addAndGet(delta)	јналог incrementAndGet(), но увеличение происходит на delta.
-   getAndAdd(delta)	јналог getAndIncrement(), но увеличение происходит на delta.
ѕример [ZooManager] ConcurrentHashMap
ѕример [Threads.Blocking.Bank2] AtomicInteger